import React from "react";

const PanelSearch = () => {
  return (
    <div className="panel--search-container">
      <form onSubmit={() => {}} >
        <input type="text" placeholder="Buscar..." className="panel--search-input" />
      </form>
    </div>
  );
};

export default PanelSearch;
